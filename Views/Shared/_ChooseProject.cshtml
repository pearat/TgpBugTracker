@model IEnumerable<TgpBugTracker.Models.ProjectIdName>

@{
    ViewBag.Title = "Ticket List";
}

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()

    <div class="form-horizontal">

        @*<div class="form-group" style="margin-bottom:0; padding-top:5px">
            @Html.LabelFor(m => m.Name, "Project Name", htmlAttributes: new { @class = "control-label col-md-2" })
            <div class="col-md-10" style="padding-left:15px">
                @Html.DropDownList("ProjectId", null, htmlAttributes: new { @class = "form-control" })

                @Html.ValidationMessageFor(m => m.Name, "", new { @class = "text-danger" })
            </div>
        </div>

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="ChooseProject" class="btn btn-default" />
            </div>
        </div>*@
    </div>
}
